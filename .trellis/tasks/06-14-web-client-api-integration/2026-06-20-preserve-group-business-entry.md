# 2026-06-20 保留群业务入口

## 背景

- 用户明确要求：群业务入口仍要保留，后续由用户调整接口实现。
- 当前后端暂未兼容 OpenIM `groupID` 作为业务 `roomId`，但这不应影响前端入口展示。

## 调整

- `GroupSettings` 不再把 `businessRoomId` 是否存在作为 `GroupBusinessEntrances` 的渲染门槛。
- 传参仍保持既有策略：显式业务 `roomId` 优先，缺失时使用 OpenIM `groupID` 兜底。

## 边界

- 本次只调整入口展示条件，不新增业务接口，不改写后端契约。
- 未运行单元测试、构建或验证脚本。
- 未触发发送、上传、下载、删除、审核、清空、退出、转让、设管理员等真实 mutation 或下载动作。
