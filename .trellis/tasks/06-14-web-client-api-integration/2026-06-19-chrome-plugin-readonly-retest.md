# 2026-06-19 Chrome 插件只读复测

## 范围

本轮使用已安装的 Codex Chrome 扩展连接真实 Chrome 进行只读复测。未运行单元测试、未运行验证脚本，未修改测试文件。

## 处理与确认

- 已通过 Chrome 扩展识别真实 Chrome profile `旺旺`，并新建受控页签打开本地 Web 页面。
- 登录账号：`18888888888`，企业号固定 `LOCALTEST001`。
- 重新从左侧会话列表进入群 `啊啊i`，URL 为 `#/chat/sg_4011035808`。
- 群设置抽屉正常打开，显示：
  - 群名称：`啊啊i`
  - 群 ID：`4011035808`
  - 群成员数：`3`
  - 群免打扰、群置顶、清空聊天记录、群权限、消息销毁、阅后即焚等入口
- 聊天资源弹窗正常打开：
  - 收藏消息为空态
  - 群共享文件为空态
- 聊天记录搜索弹窗正常打开，搜索 `qq` 后为空态。

## 结论

- 通过真实 Chrome 插件复测，群设置、聊天资源、群共享文件、群聊天记录搜索只读链路均无控制台 warn/error。
- 未观察到 `请求参数验证失败`、CORS 或 roomId 相关业务错误。
- 本轮未触发发送消息、上传文件、下载文件、收藏、撤回、清空聊天记录、修改群设置、处理群申请等 mutation 或下载动作。

## 额外发现

- 直接访问 `#/chat/sg_3413653759` 时，消息区可按 URL 加载，但头部设置入口仍可能使用旧的 `currentConversation`，导致打开单聊设置。
- 从左侧会话列表点击群后，`currentConversation` 能正确同步，群设置入口恢复正常。
- 该现象更偏向路由直达和会话 store 同步问题，不是本轮 businessApi roomId 字段接入问题；后续如需支持刷新/直达群会话，需要补齐 URL conversationID 到 `currentConversation` 的同步逻辑。

## 代码状态说明

- `pickExplicitBusinessRoomId` 已支持从明确业务字段、嵌套对象以及 `ex` JSON 中提取旧系统业务 `roomId`。
- 群设置、群资源、聊天记录搜索、发送前校验、文件上传和消息右键等链路继续使用显式业务 `roomId`，避免把 OpenIM SDK `groupID` 当旧系统 `roomId` 传给 `/room/openim/**`。
