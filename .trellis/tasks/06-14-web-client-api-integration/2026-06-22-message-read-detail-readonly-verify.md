# 2026-06-22 群消息已读详情只读复测

## 范围

- 本轮只复测群消息右键菜单中的只读入口：`消息已读列表`。
- 不触发转发、收藏、删除、撤回、发送或其他真实 mutation。

## 浏览器状态

- 页面：`http://127.0.0.1:7777/index.html#/chat/sg_3413653759`
- 群名：`橙子皮、橙子皮1、橙子皮4`
- 可见群消息：
  - 发送者：`橙子皮1`
  - 时间：`17:48`
  - 内容区域显示：`[暂未支持的消息类型]`

## 操作

- 关闭聊天资源弹窗和容量概览弹窗，避免浮层拦截。
- 右键群消息节点。
- 菜单显示：
  - `转发`
  - `收藏`
  - `消息已读列表`
  - `删除`
- 仅点击 `消息已读列表`。

## 请求结果

- 触发请求：
  - `POST /business-api/room/openim/message/read-detail?roomId=3413653759&clientMsgID=2ed75e14a3c07c61cd460f750e30597a&serverMsgID=9ae97613640b4702d47b6b00e317ab32&seq=6&access_token=...`
- HTTP 状态：200。
- 响应体：
  - `resultCode=1010101`
  - `resultMsg="请求参数验证失败，缺少必填参数或参数错误"`
- 页面表现：
  - 弹窗标题为 `消息已读列表`。
  - 页面展示空态 `未搜索到相关结果`。
- Playwright console error 复查为 0。

## 结论

- 群消息已读详情入口已完成真实浏览器只读触发验证，不再处于“无可右键群消息节点”的阻塞状态。
- 前端已按只读兜底展示空态，没有弹用户级错误 toast。
- 后端仍未兼容 OpenIM `groupID=3413653759` 作为 `/room/openim/message/read-detail` 的业务 `roomId`，与群成员、群共享文件、群容量概览的契约风险一致。
- 本轮未触发转发、收藏、删除、撤回、发送或其他真实 mutation。
