# 2026-06-24 固定企业号误判记录

## 范围

本记录原先误判固定企业号应为 `C3PY9DYPU`。2026-06-25 用户明确说明 `DEFAULT_ENTERPRISE_CODE = "LOCALTEST001"` 是其主动修改且正确的当前配置，因此本记录只保留为误判审计，不作为当前有效契约。

## 当前结论

- 当前源码有效值：`DEFAULT_ENTERPRISE_CODE = "LOCALTEST001"`。
- 登录、注册、验证码发送、验证码校验、企业号校验继续通过该常量使用固定企业号，不允许表单输入覆盖。
- 根目录当前状态文档已恢复为 `LOCALTEST001`。

## 边界

- 不删除本文件，避免隐藏此前误判；以后读取当前契约时应以 2026-06-25 纠正记录和源码为准。
- 未改写早期 Trellis 真实浏览器复测记录中出现的 `LOCALTEST001`，这些记录反映当时实际请求和历史状态。

## 验收状态

- 本轮未运行单元测试、构建、覆盖检查或验证脚本。
- 本轮未打开浏览器，也未触发 `/enterprise/code/validate`、`/account/login`、`/account/register`、验证码接口或其它真实请求。
- 当前仅完成文档纠错记录。
