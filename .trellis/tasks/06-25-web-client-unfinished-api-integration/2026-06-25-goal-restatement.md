# 2026-06-25 需求目标重申

## 用户目标

继续推进 OpenIM Electron Demo 的 Web 用户端接口接入，把当前仍未完成、但属于 Web 用户端需求范围的接口调用补齐到真实页面或调用链中。

## 接口环境

- `businessApi`: `http://47.238.134.161:8092`
- `openIMApiURL`: `http://47.238.134.161:10002`
- `openIMWsURL`: `ws://47.238.134.161:10001`
- 接口文档：`http://47.238.134.161:8092/openim-swagger.html`
- 本地 Swagger JSON：`docs/openim-swagger.json`

## 接入边界

- Web 用户端业务接口走 `businessApi`，包括登录、注册、添加好友、好友查询、加群、群业务同步、退出等。
- 聊天消息收发、会话、连接和即时通信能力继续走 OpenIM SDK，对应 `openIMApiURL` 和 `openIMWsURL`。
- 本次任务不接入后台、支付、运营、直播、客服、运维桥接等非 Web 用户端接口。

## 关键约束

- `DEFAULT_ENTERPRISE_CODE = "LOCALTEST001"` 是用户特意指定的企业号，不允许改回其它值。
- `invitationCode` 按非必填处理。
- 当前是测试数据，用户已授权真实调用接口、触发写操作、修改测试数据。
- 不新增或修改单元测试，不跑单元测试；验证以浏览器复测和静态核对为主。

## 当前推进方式

- 以当前 Trellis 任务 `06-25-web-client-unfinished-api-integration` 为主任务继续记录。
- 每接入一个接口，需要记录是否已落到调用链、是否完成浏览器复测、真实响应结果或失败原因。
- 已接入但未浏览器确认的接口先标记为“已接入，待复测/待后端响应确认”。

## 2026-06-25 最新重申

用户当前明确要求：继续在当前 Trellis 任务中推进“Web 用户端未完成接口调用接入”，目标不是重做已完成能力，而是在已完成登录、注册、好友、群、资料、设置、举报、退出等基础接入的前提下，继续梳理并实现仍属于 Web 用户端范围、且当前页面或调用链可以承载的业务接口。

本轮目标边界如下：

- 只接入 Web 用户端接口，不接后台管理、运营、支付、运维桥接、开放平台等非 Web 用户端能力。
- 业务类接口继续走 `businessApi: http://47.238.134.161:8092`。
- 聊天消息收发、会话、连接、实时通信能力继续走 OpenIM SDK，对应 `openIMApiURL` 和 `openIMWsURL`。
- 对 Swagger 中存在但当前 Web 端没有自然入口、没有页面承载、或会引入较大产品改造的接口，先记录为“暂不接/待产品入口”，不为接接口而新增大模块。
- 当前测试数据允许真实调用接口、触发写操作、修改测试数据。
- 后续验证方式以浏览器复测和静态核对为主，不新增、修改或运行单元测试。
- `DEFAULT_ENTERPRISE_CODE = "LOCALTEST001"` 是用户指定值，必须保留。
- `invitationCode` 按非必填处理。

下一步执行要求：

1. 继续从 `docs/openim-swagger.json` 和线上文档核对未完成接口。
2. 按“已有 UI/调用链可承载优先”的原则接入。
3. 每个接口记录状态：已接入且可用、已接入待浏览器确认、未实现接入、暂不接及原因。
4. 对真实调用失败的接口，记录请求路径、请求参数、响应结果和失败判断，不用单元测试替代浏览器验证。
