# 2026-06-26 静态与运行态复核

## 本轮变更

- 校验 `/user/outtime` 登录态检查接入已落盘。
- 修正 Trellis 与根目录汇总中 `/room/add` 的过时描述。
- 移除业务建群成功但无明确群 ID 时按群名称兜底跳转的逻辑，避免同名群误跳。

## 当前运行态

- 已重新启动 Web dev server。
- `127.0.0.1:7777` 当前监听中。
- `http://127.0.0.1:7777/index.html#/login` 返回 HTTP 200。
- `http://127.0.0.1:7777/business-api/openim-swagger.html` 返回 HTTP 200。

## 静态核对

- `src/api/**` 当前封装唯一业务路径数：`162`。
- 未发现冲突标记。
- 未新增、未修改、未运行单元测试。
