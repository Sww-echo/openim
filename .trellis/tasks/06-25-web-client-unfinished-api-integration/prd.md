# Web 用户端未完成接口调用接入

## 范围

本任务是 `06-14-web-client-api-integration` 的后续任务，只处理 Web 用户端已识别但尚未完成接入的接口调用。

继续不接入后台、支付、运营、直播、客服、运维桥接等非 Web 用户端接口。

## 需求目标重申

本轮目标是继续完成 OpenIM Electron Demo 的 Web 用户端接口接入，基于本地已保存的 Swagger 文档 `docs/openim-swagger.json` 和线上接口文档 `http://47.238.134.161:8092/openim-swagger.html` 对齐当前代码实现。

接口地址保持为：

- `businessApi`: `http://47.238.134.161:8092`
- `openIMApiURL`: `http://47.238.134.161:10002`
- `openIMWsURL`: `ws://47.238.134.161:10001`

接入分工保持为：

- 登录、注册、添加好友、好友查询、加群、群业务同步、退出等业务能力优先走 `businessApi`。
- 聊天消息收发、会话、OpenIM 即时通信能力继续走 OpenIM SDK，对应 `openIMApiURL` 和 `openIMWsURL`。
- 当前任务只补齐 Web 用户端可落地的业务接口调用，不主动实现后台管理、运营治理、支付、运维桥接等非 Web 用户端能力。

已确认的项目约束：

- `DEFAULT_ENTERPRISE_CODE = "LOCALTEST001"` 是用户指定值，必须保留。
- `invitationCode` 非必填。
- 当前数据为测试数据，允许真实调用接口、触发写操作、修改测试数据。
- 不新增或修改单元测试，不以单元测试作为本项目验证方式；后续以浏览器复测和静态核对为主。

## 用户授权

用户明确说明当前是测试数据，并授权：

- 可以真实调用业务接口。
- 可以触发写操作或修改测试数据。
- 可以用浏览器复测 mutation 类接口。

## 首批目标

1. `/user/logout`
   - 接入正常手动退出链路。
   - 按 Swagger 传 `deviceKey/devicekey/telephone/access_token`，`access_token` 由请求拦截器追加。
   - 业务退出失败不应阻断本地退出清理，避免用户无法退出。

2. `/room/add`
   - 复核 Swagger 旧系统建群契约。
   - 在不破坏当前 OpenIM SDK 建群体验的前提下，尝试补业务建群调用或记录真实接口不可用原因。

3. 其它 Web 用户端候选
   - 从旧 Trellis 中“待后端契约明确后再接”“待产品入口后再接”的接口逐项复核。
   - 只纳入 Web 用户端有明确入口或可合理映射到当前页面的能力。

## 验收方式

- 允许真实浏览器调用测试接口。
- 每个接口记录：是否已接入、是否已发起真实请求、响应结果、失败原因、是否还有后续工作。
- 不新增或修改单元测试；本项目后续验证以浏览器复测和静态核对为主。
